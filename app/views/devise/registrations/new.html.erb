<h2 class="NXVPg Szr5J  coreSpriteLoggedOutWordmark">Instagram</h2>
<h4>友達の写真や動画をチェックしよう</h4>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder:"メールアドレス" %>
  </div>
  
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, placeholder:"フルネーム" %>
  </div>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username, placeholder:"ユーザーネーム" %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password", placeholder:"パスワード" %>
  </div>

  <div class="actions">
    <%= f.submit "登録する", class:"btn btn-md btn-primary" %>
  </div>
<% end %>

登録することで、Facebookの利用規約、データに関するポリシー、Cookieポリシーに同意するものとします。<br>

<%= render "devise/shared/links" %>
